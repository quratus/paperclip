schema: "paperclip/v1"

agents:
  repo-janitor:
    role: "engineer"
    capabilities: "Stale branch cleanup, dependency updates, changelog generation, README maintenance, automated repo hygiene fixes"
    adapter:
      type: kimi_local
      config:
        agentPreset: custom
        customAgentFile: /Users/JuliusHalm 1/workspace/paperclip/packages/companies/sqncr-repo-janitor/agents/repo-janitor/kimi/agent.yaml
        cwd: /Users/JuliusHalm 1/workspace/brain-platform
        timeoutSec: 1800
    runtime:
      heartbeat:
        # Timer off by design — driven by the "Daily repo hygiene sweep" routine,
        # which creates a task and wakes this agent via wakeOnDemand.
        enabled: false
        wakeOnDemand: true
